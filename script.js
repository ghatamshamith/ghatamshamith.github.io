const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu.onclick=()=>nav.classList.toggle('open');nav.querySelectorAll('a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));
const ps=[...document.querySelectorAll('.photo')],box=document.querySelector('.lightbox'),im=box.querySelector('img'),cnt=box.querySelector('.count');let i=0;function show(n){i=(n+ps.length)%ps.length;im.src=ps[i].dataset.img;im.alt=ps[i].querySelector('img').alt;cnt.textContent=`0${i+1} / 0${ps.length}`;box.classList.add('open');document.body.style.overflow='hidden'}function close(){box.classList.remove('open');document.body.style.overflow=''}ps.forEach((p,n)=>p.onclick=()=>show(n));box.querySelector('.x').onclick=close;box.querySelector('.prev').onclick=()=>show(i-1);box.querySelector('.next').onclick=()=>show(i+1);box.onclick=e=>{if(e.target===box)close()};document.onkeydown=e=>{if(!box.classList.contains('open'))return;if(e.key==='Escape')close();if(e.key==='ArrowLeft')show(i-1);if(e.key==='ArrowRight')show(i+1)};
